<resources>

    <!-- general styles for the action bar -->
	<declare-styleable name="AppTheme">
	</declare-styleable>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <!--<item name="actionBarStyle">@style/ActionBar</item>-->
    </style>
    <style name="LoadActivityTheme" parent="@style/Theme.AppCompat.Light">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/loadBackground</item>
    </style>
    <!-- Application theme. -->
    <style name="ActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid">
        <item name="logo">@drawable/rsz_ab_icon</item>
        <item name="displayOptions">useLogo|showHome</item>
    </style>

    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="actionBarStyle">@style/ActionBar</item>

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

</resources>